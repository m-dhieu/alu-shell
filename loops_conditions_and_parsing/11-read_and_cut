#!/usr/bin/env bash 
# Script to read /etc/passwd and display the username, user ID, and home directory path for the user.

while IFS=: read -r username password uid gid gecos homedir shell; do
    echo "Username: $username"
    echo "User ID: $uid"
    echo "Home Directory: $homedir"
done < /etc/passwd
